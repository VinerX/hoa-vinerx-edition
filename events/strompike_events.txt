###########################
#stormpike

###########################

add_namespace = stormpike

country_event = { # stormwind invites us to the alliance
  id = stormpike.1
  title = stormpike.1.t
  desc = stormpike.1.d

  is_triggered_only = yes

  fire_only_once = yes

  option = { #join the alliance
    name = stormpike.1.option.1
    IF = {
			Limit = { STO = { has_government = alliance } }
			add_popularity = {
				ideology = alliance
				popularity = 0.05
			}
		}
		IF = {
			Limit = { STO = { has_government = neutral } }
			add_popularity = {
				ideology = neutral
				popularity = 0.05
			}
		}
    if = {
      limit = {
        ROOT = { tag = AER }
      }
      STO = { complete_national_focus = STO_second_war_gryphon_riders }
    }
    if = {
      limit = {
        ROOT = { tag = SPC }
      }
      STO = { complete_national_focus = STO_stormpike_metalworkers }
    }

  }
  
   option = { #better off alone
    name = stormpike.1.option.2
    hidden_effect = {
      STO = { add_political_power = 30 }
    }

  }
}