add_namespace = hoa.tutorial

country_event = { #Tutorial Event, fired at Gamestart
	id = hoa.tutorial.1
	title = hoa.tutorial.1.t
	desc = hoa.tutorial.1.d
	#picture = 

	is_triggered_only = yes

	# Tell me about Races
	option = {
		name = hoa.tutorial.1.option.2
		country_event = hoa.tutorial.2
		# Failsafe in the very unlikely but possible case the AI get this event accidentally somehow via PDX spaghetti code, makes sure they exit
		ai_chance = {
			base = 0
		}
	}

	# Tell me about Faith and Religions
	option = {
		name = hoa.tutorial.1.option.3
		country_event = hoa.tutorial.3
		# Failsafe in the very unlikely but possible case the AI get this event accidentally somehow via PDX spaghetti code, makes sure they exit
		ai_chance = {
			base = 0
		}
	}

	# Tell me about Coring States
	option = {
		name = hoa.tutorial.1.option.4
		country_event = hoa.tutorial.4
		# Failsafe in the very unlikely but possible case the AI get this event accidentally somehow via PDX spaghetti code, makes sure they exit
		ai_chance = {
			base = 0
		}
	}


	# No other help needed (closes guide)
	option = {
		name = hoa.tutorial.1.option.1
	}
}

country_event = { #Races Tutorial Part 1
	id = hoa.tutorial.2 #
	title = hoa.tutorial.2.t
	desc = hoa.tutorial.2.d
	picture = GFX_races_tutorial_image_1

	is_triggered_only = yes

	# Return to Menu
	option = {
		name = hoa.tutorial.2.option.2
		country_event = hoa.tutorial.1
		# Failsafe in the very unlikely but possible case the AI get this event accidentally somehow via PDX spaghetti code, makes sure they exit
		ai_chance = {
			base = 0
		}
	}

	# No other help needed (closes guide)
	option = {
		name = hoa.tutorial.2.option.1
	}
}

country_event = { #Religions and Faith Tutorial Part 1
	id = hoa.tutorial.3 #
	title = hoa.tutorial.3.t
	desc = hoa.tutorial.3.d
	picture = GFX_religion_tutorial_image_1

	is_triggered_only = yes

	# Continue
	option = {
		name = hoa.tutorial.3.option.2
		country_event = hoa.tutorial.5
		# Failsafe in the very unlikely but possible case the AI get this event accidentally somehow via PDX spaghetti code, makes sure they exit
		ai_chance = {
			base = 0
		}
	}

	# Return to Menu
	option = {
		name = hoa.tutorial.3.option.3
		country_event = hoa.tutorial.1
		# Failsafe in the very unlikely but possible case the AI get this event accidentally somehow via PDX spaghetti code, makes sure they exit
		ai_chance = {
			base = 0
		}
	}

	# No other help needed (closes guide)
	option = {
		name = hoa.tutorial.3.option.1
	}
}

country_event = { #Coring States Tutorial Part 1
	id = hoa.tutorial.4 #
	title = hoa.tutorial.4.t
	desc = hoa.tutorial.4.d
	picture = GFX_coring_tutorial_image_1

	is_triggered_only = yes

	# Return to Menu
	option = {
		name = hoa.tutorial.4.option.2
		country_event = hoa.tutorial.1
		# Failsafe in the very unlikely but possible case the AI get this event accidentally somehow via PDX spaghetti code, makes sure they exit
		ai_chance = {
			base = 0
		}
	}

	# No other help needed (closes guide)
	option = {
		name = hoa.tutorial.4.option.1
	}
}

country_event = { #Religions and Faith Tutorial Part 2
	id = hoa.tutorial.5 #
	title = hoa.tutorial.5.t
	desc = hoa.tutorial.5.d
	picture = GFX_religion_tutorial_image_2

	is_triggered_only = yes

	# Continue
	option = {
		name = hoa.tutorial.5.option.2
		country_event = hoa.tutorial.6
		# Failsafe in the very unlikely but possible case the AI get this event accidentally somehow via PDX spaghetti code, makes sure they exit
		ai_chance = {
			base = 0
		}
	}

	# Return to Menu
	option = {
		name = hoa.tutorial.5.option.3
		country_event = hoa.tutorial.1
		# Failsafe in the very unlikely but possible case the AI get this event accidentally somehow via PDX spaghetti code, makes sure they exit
		ai_chance = {
			base = 0
		}
	}

	# No other help needed (closes guide)
	option = {
		name = hoa.tutorial.5.option.1
	}
}

country_event = { #Religions and Faith Tutorial Part 3
	id = hoa.tutorial.6 #
	title = hoa.tutorial.6.t
	desc = hoa.tutorial.6.d
	picture = GFX_religion_tutorial_image_3

	is_triggered_only = yes

	# Return to Menu
	option = {
		name = hoa.tutorial.6.option.2
		country_event = hoa.tutorial.1
		# Failsafe in the very unlikely but possible case the AI get this event accidentally somehow via PDX spaghetti code, makes sure they exit
		ai_chance = {
			base = 0
		}
	}

	# No other help needed (closes guide)
	option = {
		name = hoa.tutorial.6.option.1
	}
}