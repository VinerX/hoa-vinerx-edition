STC_war_contracts = {

	STC_naval_contract = {

		priority = 10
		cost = 100
		fire_only_once = yes

		icon = generic_navy_bonus

		allowed = { original_tag = STC }

		visible = { is_in_faction_with = BRC }
		available = { is_in_faction_with = BRC }

		days_remove = 1

		complete_effect = {
			add_timed_idea = {
				idea = STC_naval_boom
				days = 365
			}
			BRC = {
				every_country = {
					limit = {
						is_in_faction_with = BRC
						NOT = { original_tag = STC }
					}
					add_timed_idea = {
						idea = horde_goblin_ships
						days = 365
					}
				}
			}
			set_country_flag = STC_contracts_taken
		}

		ai_will_do = {
			factor = 20
		}
	}

	STC_airship_contract = {

		priority = 9
		cost = 100
		fire_only_once = yes

		icon = generic_air_bonus

		allowed = { original_tag = STC }

		visible = { is_in_faction_with = BRC }
		available = { is_in_faction_with = BRC }

		days_remove = 1

		complete_effect = {
			add_timed_idea = {
				idea = STC_airship_boom
				days = 365
			}
			BRC = {
				every_country = {
					limit = {
						is_in_faction_with = BRC
						NOT = { original_tag = STC }
					}
					add_timed_idea = {
						idea = horde_goblin_aircraft
						days = 365
					}
				}
			}
			set_country_flag = STC_contracts_taken
		}

		ai_will_do = {
			factor = 20
		}
	}

	STC_sappers_contract = {

		priority = 8
		cost = 75
		fire_only_once = yes

		icon = generic_construction_bonus

		allowed = { original_tag = STC }

		visible = { is_in_faction_with = BRC }
		available = { is_in_faction_with = BRC }

		days_remove = 1

		complete_effect = {
			add_timed_idea = {
				idea = STC_sappers_contract
				days = 365
			}
			BRC = {
				every_country = {
					limit = {
						is_in_faction_with = BRC
						NOT = { original_tag = STC }
					}
					add_timed_idea = {
						idea = horde_goblin_construction
						days = 365
					}
				}
			}
			set_country_flag = STC_contracts_taken
		}

		ai_will_do = {
			factor = 20
		}
	}

	STC_renew_contracts = {

		priority = 7
		cost = 100
		fire_only_once = no

		icon = generic_production

		allowed = { original_tag = STC }

		visible = {
			is_in_faction_with = BRC
			has_country_flag = STC_contracts_completed
		}
		available = {
			is_in_faction_with = BRC
			has_country_flag = STC_contracts_completed
		}

		days_remove = 1
		days_re_enable = 365

		complete_effect = {
			clr_country_flag = STC_contracts_completed
			add_timed_idea = {
				idea = STC_naval_boom
				days = 365
			}
			add_timed_idea = {
				idea = STC_airship_boom
				days = 365
			}
			add_timed_idea = {
				idea = STC_sappers_contract
				days = 365
			}
			BRC = {
				every_country = {
					limit = {
						is_in_faction_with = BRC
						NOT = { original_tag = STC }
					}
					add_timed_idea = {
						idea = horde_goblin_ships
						days = 365
					}
					add_timed_idea = {
						idea = horde_goblin_aircraft
						days = 365
					}
					add_timed_idea = {
						idea = horde_goblin_construction
						days = 365
					}
				}
			}
		}

		ai_will_do = {
			factor = 15
		}
	}
}
