STC_war_contracts = {

	icon = generic_production

	allowed = { original_tag = STC }

	visible = { is_in_faction_with = BRC }

	priority = 10

	STC_naval_contract = {

		priority = 10
		cost = 100
		fire_only_once = yes

		icon = generic_navy_bonus

		allowed = { original_tag = STC }

		visible = { is_in_faction_with = BRC }
		available = { is_in_faction_with = BRC }

		days_remove = 1

		complete_effect = {
			add_timed_idea = {
				idea = STC_naval_boom
				days = 365
			}
			BRC = {
				every_country = {
					limit = {
						is_in_faction_with = BRC
						NOT = { original_tag = STC }
					}
					add_timed_idea = {
						idea = horde_goblin_ships
						days = 365
					}
				}
			}
			set_country_flag = STC_contracts_taken
		}

		ai_will_do = {
			factor = 20
		}
	}

	STC_airship_contract = {

		priority = 9
		cost = 100
		fire_only_once = yes

		icon = generic_air_bonus

		allowed = { original_tag = STC }

		visible = { is_in_faction_with = BRC }
		available = { is_in_faction_with = BRC }

		days_remove = 1

		complete_effect = {
			add_timed_idea = {
				idea = STC_airship_boom
				days = 365
			}
			BRC = {
				every_country = {
					limit = {
						is_in_faction_with = BRC
						NOT = { original_tag = STC }
					}
					add_timed_idea = {
						idea = horde_goblin_aircraft
						days = 365
					}
				}
			}
			set_country_flag = STC_contracts_taken
		}

		ai_will_do = {
			factor = 20
		}
	}

	STC_sappers_contract = {

		priority = 8
		cost = 75
		fire_only_once = yes

		icon = generic_construction_bonus

		allowed = { original_tag = STC }

		visible = { is_in_faction_with = BRC }
		available = { is_in_faction_with = BRC }

		days_remove = 1

		complete_effect = {
			add_timed_idea = {
				idea = STC_sappers_contract
				days = 365
			}
			BRC = {
				every_country = {
					limit = {
						is_in_faction_with = BRC
						NOT = { original_tag = STC }
					}
					add_timed_idea = {
						idea = horde_goblin_construction
						days = 365
					}
				}
			}
			set_country_flag = STC_contracts_taken
		}

		ai_will_do = {
			factor = 20
		}
	}

	STC_renew_contracts = {

		priority = 7
		cost = 100
		fire_only_once = no

		icon = generic_production

		allowed = { original_tag = STC }

		visible = {
			is_in_faction_with = BRC
			has_country_flag = STC_contracts_completed
		}
		available = {
			is_in_faction_with = BRC
			has_country_flag = STC_contracts_completed
		}

		days_remove = 1
		days_re_enable = 365

		complete_effect = {
			clr_country_flag = STC_contracts_completed
			add_timed_idea = {
				idea = STC_naval_boom
				days = 365
			}
			add_timed_idea = {
				idea = STC_airship_boom
				days = 365
			}
			add_timed_idea = {
				idea = STC_sappers_contract
				days = 365
			}
			BRC = {
				every_country = {
					limit = {
						is_in_faction_with = BRC
						NOT = { original_tag = STC }
					}
					add_timed_idea = {
						idea = horde_goblin_ships
						days = 365
					}
					add_timed_idea = {
						idea = horde_goblin_aircraft
						days = 365
					}
					add_timed_idea = {
						idea = horde_goblin_construction
						days = 365
					}
				}
			}
		}

		ai_will_do = {
			factor = 15
		}
	}
}

STC_fleet_delivery = {

	icon = generic_navy_bonus

	allowed = { original_tag = STC }

	visible = {
		is_in_faction_with = BRC
		has_country_flag = STC_contracts_taken
		has_war_with = LOR
	}

	priority = 15

	STC_deploy_steamwheedle_battleships = {
		priority = 10
		cost = 80
		fire_only_once = yes
		icon = generic_navy_bonus
		allowed = { original_tag = STC }
		visible = {
			is_in_faction_with = BRC
			has_idea = STC_naval_boom
		}
		available = {
			is_in_faction_with = BRC
			has_idea = STC_naval_boom
			NOT = { has_country_flag = STC_battleships_delivered }
		}
		days_remove = 1
		complete_effect = {
			set_country_flag = STC_battleships_delivered
			country_event = { id = steamwheedle_fleet.1 }
		}
		ai_will_do = { factor = 15 }
	}

	STC_deploy_amani_frigates = {
		priority = 9
		cost = 60
		fire_only_once = yes
		icon = generic_navy_bonus
		allowed = { original_tag = STC
			AMA = { is_in_faction_with = BRC }
		}
		visible = {
			is_in_faction_with = BRC
			has_idea = STC_naval_boom
		}
		available = {
			is_in_faction_with = BRC
			has_idea = STC_naval_boom
			NOT = { has_country_flag = STC_amani_frigates_delivered }
		}
		days_remove = 1
		complete_effect = {
			set_country_flag = STC_amani_frigates_delivered
			country_event = { id = steamwheedle_fleet.2 }
		}
		ai_will_do = { factor = 15 }
	}

	STC_deploy_frostmane_destroyers = {
		priority = 8
		cost = 60
		fire_only_once = yes
		icon = generic_navy_bonus
		allowed = { original_tag = STC }
		visible = {
			is_in_faction_with = BRC
			has_idea = STC_naval_boom
		}
		available = {
			is_in_faction_with = BRC
			has_idea = STC_naval_boom
			NOT = { has_country_flag = STC_frostmane_destroyers_delivered }
		}
		days_remove = 1
		complete_effect = {
			set_country_flag = STC_frostmane_destroyers_delivered
			country_event = { id = steamwheedle_fleet.3 }
		}
		ai_will_do = { factor = 15 }
	}

	STC_deploy_dreadmaul_battleships = {
		priority = 7
		cost = 80
		fire_only_once = yes
		icon = generic_navy_bonus
		allowed = { original_tag = STC }
		visible = {
			is_in_faction_with = BRC
			has_idea = STC_naval_boom
		}
		available = {
			is_in_faction_with = BRC
			has_idea = STC_naval_boom
			NOT = { has_country_flag = STC_dreadmaul_battleships_delivered }
		}
		days_remove = 1
		complete_effect = {
			set_country_flag = STC_dreadmaul_battleships_delivered
			country_event = { id = steamwheedle_fleet.4 }
		}
		ai_will_do = { factor = 15 }
	}

	STC_emergency_shipments = {
		priority = 6
		cost = 50
		fire_only_once = no
		icon = generic_navy_bonus
		allowed = { original_tag = STC }
		visible = {
			is_in_faction_with = BRC
			has_idea = STC_naval_boom
		}
		available = {
			is_in_faction_with = BRC
			has_idea = STC_naval_boom
			BRC = {
				has_navy_size = { size < 30 }
				has_war_with = LOR
			}
		}
		days_remove = 1
		days_re_enable = 180
		complete_effect = {
			BRC = {
				if = {
					limit = { has_navy_size = { size < 20 } }
					country_event = { id = steamwheedle_fleet.5 }
				}
				else = {
					add_political_power = 25
				}
			}
		}
		ai_will_do = { factor = 10 }
	}
}
