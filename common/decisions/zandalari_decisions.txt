government_decisions = {

	ZAN_kill_rastakhan = {

		allowed = { original_tag = ZAN }
		available = { NOT = { has_government = neutral } }
		visible = { original_tag = ZAN }

		fire_only_once = yes

		ai_will_do = { factor = 1 }

		cost = 25

		complete_effect = {
			kill_country_leader = yes
			remove_ideas = ZAN_Rastakhans_Iron_Fist
		}
	}

}