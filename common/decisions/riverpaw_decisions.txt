formable_category = { 

	RPP_gnoll_recovery_1 = {

        priority = 10

        cost = 10

		fire_only_once = yes

        allowed = { original_tag = RPP }

        visible = {
			tag = RPP
        }

        available = {
			RPP = {	num_of_controlled_states > 3 }
			has_idea = RPP_aftermath_of_the_gnoll_wars
        }

        complete_effect = {
			swap_ideas = {
				remove_idea = RPP_aftermath_of_the_gnoll_wars
				add_idea = RPP_aftermath_of_the_gnoll_wars_2
			}
		}
    }

	RPP_gnoll_recovery_2 = {

        priority = 10

        cost = 10

        allowed = { original_tag = RPP }

        visible = {
			tag = RPP
        }

        available = {
			RPP = {	num_of_controlled_states > 11 }
			has_idea = RPP_aftermath_of_the_gnoll_wars_2
        }

        complete_effect = {
			swap_ideas = {
				remove_idea = RPP_aftermath_of_the_gnoll_wars_2
				add_idea = RPP_aftermath_of_the_gnoll_wars_3
			}
		}
    }

	RPP_gnoll_recovery_3 = {

        priority = 10

        cost = 10

        days_re_enable = 30

        allowed = { original_tag = RPP }

        visible = {
			tag = RPP
        }

        available = {
			RPP = {	num_of_controlled_states > 23 }
			has_idea = RPP_aftermath_of_the_gnoll_wars_3
        }

        complete_effect = {
			remove_ideas = RPP_aftermath_of_the_gnoll_wars_3
		}
    }
}