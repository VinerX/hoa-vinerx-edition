#Used for modifying faith gain by a flat amount
country_faith_gain_flat = {
	color_type = good
	value_type = number
	precision = 1
	category = country
}
#Used for modifying faith gain by a percentage
country_faith_gain_factor = {
	color_type = good
	value_type = percentage
	precision = 1
	category = country
} 