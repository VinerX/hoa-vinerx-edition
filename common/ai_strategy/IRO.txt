####Isolation Country minor independence
IRO_resource_independence_wheat = {
	allowed = { original_tag = IRO }
	enable = {
		NOT = {	IRO = { farm > 3 } }
		date < 595.1.1
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = building_target
		id = farm
		value = 4
	}
}

IRO_resource_independence_oil = {
	allowed = { original_tag = IRO }
	enable = {
		NOT = {	IRO = { synthetic_refinery > 1 } }
		date < 613.1.1
		date > 600.1.1
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = building_target
		id = synthetic_refinery
		value = 2
	}
}

IRO_resource_independence_gunpowder = {
	allowed = { original_tag = IRO }
	enable = {
		NOT = {	IRO = { sulfur_mine > 4 } }
		date < 611.1.1
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = building_target
		id = sulfur_mine
		value = 5
	}
}