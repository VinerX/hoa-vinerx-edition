####Isolation Country minor independence
QUE_resource_independence_iron = {
	allowed = { original_tag = QUE }
	enable = {
		NOT = {	QUE = { iron_mine > 3 } }
		date < 595.1.1
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = building_target
		id = iron_mine
		value = 4
	}
}

QUE_resource_independence_gunpowder = {
	allowed = { original_tag = QUE }
	enable = {
		NOT = {	QUE = { sulfur_mine > 1 } }
		date < 595.1.1
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = building_target
		id = sulfur_mine
		value = 2
	}
}

# Second War — Thalassian navy (reluctant but participated, targets nearby Horde)
QUE_naval_invasion_horde = {
	allowed = { original_tag = QUE }
	enable = {
		is_in_faction_with = LOR
		has_war_with = BRC
	}
	abort_when_not_enabled = yes

	# Light naval production — elves send a token fleet
	ai_strategy = {
		type = unit_ratio
		id = screen_ship
		value = 15
	}
	ai_strategy = {
		type = unit_ratio
		id = convoy
		value = 10
	}

	# Cautious invasion focus — King Anasterian was reluctant
	ai_strategy = {
		type = naval_invasion_focus
		value = 35
	}

	# Dragonmaw Port — closest Horde naval base to Quel'Thalas
	ai_strategy = {
		type = invasion_unit_request
		state = 503
		value = 65
	}
	ai_strategy = {
		type = invade
		id = "DMC"
		value = 8
	}

	# Misty Isle — DMC-held island
	ai_strategy = {
		type = invasion_unit_request
		state = 172
		value = 45
	}
	ai_strategy = {
		type = invade
		id = "DMC"
		value = 5
	}
}