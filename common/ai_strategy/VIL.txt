####Minor Resource Independence Start
VIL_resource_independence_gunpowder = {
	allowed = { original_tag = VIL }
	enable = {
		NOT = {	VIL = { farm > 1 } }
		date < 595.1.1
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = building_target
		id = farm
		value = 2
	}
}