####Minor Resource Independence Start
STR_resource_independence_food = {
	allowed = { original_tag = STR }
	enable = {
		NOT = {	STR = { farm > 5 } }
		date < 595.1.1
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = building_target
		id = farm
		value = 6
	}
}

STR_resource_independence_iron = {
	allowed = { original_tag = STR }
	enable = {
		NOT = {	STR = { iron_mine > 3 } }
		date < 595.1.1
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = building_target
		id = iron_mine
		value = 4
	}
}