####Resource Independence Start
TWV_resource_independence_mana = {
	allowed = { original_tag = TWV }
	enable = {
		NOT = {	TWV = { arcane_tower > 0 } }
		date < 595.1.1
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = building_target
		id = arcane_tower
		value = 1
	}
}