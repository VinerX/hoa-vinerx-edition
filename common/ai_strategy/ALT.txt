####Resource Independence Start
ALT_resource_independence_wheat = {
	allowed = { original_tag = ALT }
	enable = {
		NOT = {	ALT = { farm > 2 } }
		date < 595.1.1
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = building_target
		id = farm
		value = 3
	}
}

ALT_resource_independence_iron = {
	allowed = { original_tag = ALT }
	enable = {
		NOT = {	ALT = { iron_mine > 1 } }
		date < 595.1.1
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = building_target
		id = iron_mine
		value = 2
	}
}