####Resource Independence Start
WWT_resource_independence_mana = {
	allowed = { original_tag = WWT }
	enable = {
		NOT = {	WWT = { arcane_tower > 0 } }
		date < 595.1.1
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = building_target
		id = arcane_tower
		value = 1
	}
}