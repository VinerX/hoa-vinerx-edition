####Resource Independence Start
### 3rd War
FAR_resource_independence_iron = {
	allowed = { original_tag = FAR }
	enable = {
		NOT = {	FAR = { iron_mine > 0 } }
		date > 610.1.1
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = building_target
		id = iron_mine
		value = 1
	}
}