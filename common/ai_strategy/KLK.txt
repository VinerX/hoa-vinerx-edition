####Minor Resource Independence Start
KLK_resource_independence_ore = {
	allowed = { original_tag = KLK }
	enable = {
		NOT = {	KLK = { iron_mine > 0 } }
		date < 595.1.1
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = building_target
		id = iron_mine
		value = 1
	}
}

KLK_resource_independence_wood = {
	allowed = { original_tag = KLK }
	enable = {
		NOT = {	KLK = { lumber_mill > 0 } }
		date < 595.1.1
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = building_target
		id = lumber_mill
		value = 1
	}
}