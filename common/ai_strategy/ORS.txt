####Resource Independence Start
ORS_resource_independence_ore = {
	allowed = { original_tag = ORS }
	enable = {
		NOT = {	ORS = { iron_mine > 0 } }
		date < 595.1.1
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = building_target
		id = iron_mine
		value = 1
	}
}