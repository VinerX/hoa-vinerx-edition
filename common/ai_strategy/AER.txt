####Minor Resource Independence Start
AER_resource_independence_food = {
	allowed = { original_tag = AER }
	enable = {
		NOT = {	AER = { farm > 2 } }
		date < 595.1.1
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = building_target
		id = farm
		value = 3
	}
}