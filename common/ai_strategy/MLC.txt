####Isolation Country minor independence
MLC_resource_independence_iron = {
	allowed = { original_tag = MLC }
	enable = {
		NOT = {	MLC = { iron_mine > 2 } }
		date < 595.1.1
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = building_target
		id = iron_mine
		value = 3
	}
}
