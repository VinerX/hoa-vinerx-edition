####Isolation Country minor independence
AHQ_resource_independence_mount = {
	allowed = { original_tag = AHQ }
	enable = {
		NOT = {	AHQ = { stable > 1 } }
		date < 595.1.1
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = building_target
		id = stable
		value = 2
	}
}

AHQ_resource_independence_wood = {
	allowed = { original_tag = AHQ }
	enable = {
		NOT = {	AHQ = { lumber_mill > 0 } }
		date < 595.1.1
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = building_target
		id = lumber_mill
		value = 1
	}
}