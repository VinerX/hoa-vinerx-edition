####Isolation Country minor independence
BDF_resource_independence_food = {
	allowed = { original_tag = BDF }
	enable = {
		NOT = {	BDF = { farm > 1 } }
		date < 595.1.1
	}
	abort_when_not_enabled = yes
	ai_strategy = {
		type = building_target
		id = farm
		value = 2
	}
}