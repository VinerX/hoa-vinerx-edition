ai_has_no_other_wars_or_wargoals = {
	hidden_trigger = {
		if = {
			limit = { is_ai = yes }
			has_war = no
			NOT = {
				any_country = {
					ROOT = { has_wargoal_against = PREV }
				}
			}
		}
	}
}