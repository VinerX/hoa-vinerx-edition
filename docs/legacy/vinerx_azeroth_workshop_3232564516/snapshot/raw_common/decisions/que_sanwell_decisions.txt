que_sanwell = { 
    destroy_sunwell = {
        allowed = { always = yes }
        visible = { has_war_with = QUE}
        #available = { 13 = { is_controlled_by_ROOT_or_subject } }
        cost = 100
        days_remove = 100
        remove_effect = {
            QUE = { }
        }
}
}