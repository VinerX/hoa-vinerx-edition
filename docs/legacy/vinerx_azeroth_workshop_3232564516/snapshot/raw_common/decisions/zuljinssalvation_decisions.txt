zuljinssalvation = {
	sent_troops_to_save_him = {
		# allowed = {
		# 	always = no
		# }
		allowed = {
            		OR = { 
						tag = AMA 
						tag = BRC 
						}
        	}
		activation = { 
			AMA = { 
				has_idea = AMA_Captured_leader 
				
			}
		}
		icon = decision_icon_sent_troops_to_save_him
		cost = 25
		fire_only_once = yes
		days_remove = 5
		complete_effect = {
			add_command_power = -100
		}
		remove_effect = {
			AMA = { country_event = AMA_zuljin_is_captured.3 }
			if = { 
				limit = { tag = BRC }
				BRC = { country_event = AMA_zuljin_is_captured.3 }
			}
		}
		timeout_effect = {
			AMA = {
				set_country_flag = {
					flag = zuljin_death
				}
			}
		}
		# cancel_trigger = {
		# 	AMA = { 
		# 		NO = { has_idea = { AMA_zuljin_is_captured } }
		# 	}
		# }
		is_good = yes
		selectable_mission = yes
		days_mission_timeout = 45
		ai_will_do = {
   		 	base = 0
			
			modifier = {
        			add = 50
        			AMA = { has_idea = AMA_Captured_leader }
			}

			
			modifier = {
        			add = 50
        			tag = BRC
			}
		}




	}
}
